# Django Framework
Django==4.2.7

# Database (PostgreSQL for production)
dj-database-url==2.1.0

# Django REST Framework
djangorestframework==3.14.0

# Environment variables
python-decouple==3.8

# Image processing
Pillow==10.4.0

# Static files for production
whitenoise==6.6.0

# HTTP requests for external API
requests==2.31.0

# Production server
gunicorn==21.2.0

# psycopg for PostgreSQL (Render uses PostgreSQL)
# Using psycopg3 for better Python 3.13 compatibility
psycopg[binary]==3.2.3

# MySQL client (for Railway MySQL database)
# Using PyMySQL instead of mysqlclient (no compilation needed)
PyMySQL==1.1.0
